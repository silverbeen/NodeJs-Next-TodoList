import styled from "@emotion/styled";
import { CONTAIENR } from "../../styles";

export const Wrapper = styled(CONTAIENR)`
  border: 1px solid red;
`;

export const ListWrapper = styled.div`
  display: flex;
  flex-direction: column;
`;
